<Window x:Class="DrawingTestAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Анализ рисуночного теста" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Кнопка загрузки -->
        <Button Grid.Row="0" Content="📁 Загрузить шаблон" 
                Click="LoadTemplate_Click"
                FontSize="14" Padding="15,8"
                HorizontalAlignment="Left"/>

        <!-- Изображение и статистика -->
        <Grid Grid.Row="1" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Изображение -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
                <Image x:Name="TemplateImage" 
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Border>

            <!-- Разделитель -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

            <!-- Статистика -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="StatusText" 
                               FontSize="14" 
                               Margin="0,0,0,15"
                               TextWrapping="Wrap"/>

                    <DataGrid x:Name="SegmentsGrid" 
                              AutoGenerateColumns="False" 
                              Height="400"
                              IsReadOnly="True"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="40"/>
                            <DataGridTextColumn Header="Зона" Binding="{Binding Name}" Width="120"/>
                            <DataGridTextColumn Header="Пикселей" Binding="{Binding PixelCount}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock x:Name="BoundaryText" 
                               FontSize="12" 
                               Margin="0,15,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Кнопка сохранения -->
        <Button Grid.Row="2" Content="💾 Сохранить данные" 
                Click="SaveData_Click"
                FontSize="14" Padding="15,8"
                HorizontalAlignment="Left"
                Margin="0,15,0,0"/>
    </Grid>
</Window>
